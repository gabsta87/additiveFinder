import { HeaderTitlePipe } from './header-title.pipe';

describe('HeaderTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new HeaderTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
