import { DangerColorPipe } from './danger-color.pipe';

describe('DangerColorPipe', () => {
  it('create an instance', () => {
    const pipe = new DangerColorPipe();
    expect(pipe).toBeTruthy();
  });
});
