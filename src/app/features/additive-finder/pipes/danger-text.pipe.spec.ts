import { DangerTextPipe } from './danger-text.pipe';

describe('DangerTextPipe', () => {
  it('create an instance', () => {
    const pipe = new DangerTextPipe();
    expect(pipe).toBeTruthy();
  });
});
