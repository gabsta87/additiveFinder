import { DangerClassColorPipe } from './danger-class-color.pipe';

describe('DangerClassColorPipe', () => {
  it('create an instance', () => {
    const pipe = new DangerClassColorPipe();
    expect(pipe).toBeTruthy();
  });
});
